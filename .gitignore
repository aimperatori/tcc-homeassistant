# An * ensures that everything will be ignored.
*
# You can whitelist files/folders with !, these will not be ignored.
!*.yaml
!.gitignore
!*.md

# Ignore folders.
.storage
.cloud
.google.token

# Ensure these YAML files are ignored, otherwise your secret data/credentials will leak.
ip_bans.yaml
secrets.yaml
known_devices.yaml

# Custom Components
!custom_components
!custom_components/ha2firebase
!custom_components/ha2firebase/__init__.py
!custom_components/ha2firebase/README.md
!custom_components/ha2firebase/manifest.json